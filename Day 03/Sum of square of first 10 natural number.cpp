#include<stdio.h>
//Sum of square of first 10 natural number
#include<math.h>
int main()
{
	float sum;
	sum=pow(1,2)+pow(2,2)+pow(3,2)+pow(4,2)+pow(5,2)+pow(6,2)+pow(7,2)+pow(8,2)+pow(9,2)+pow(10,2);
	printf ("The sum of square of first 10 natural number is :%.2f",sum);
	return 0;
}
